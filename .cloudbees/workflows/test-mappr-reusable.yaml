apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: test mappr reusable
on:
  workflow_dispatch:
jobs:
  caller:
    uses: ./manual-test-wf.yaml
    #uses: .cloudbees/workflows/mappr-reus-wf.yaml
    with:
      instructions: "Kushal says..."
      assignee: "Demoers"