apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: appr_input_params
on:
  workflow_dispatch:
jobs:
  ask-now:
    with:
      approvers: 0a808e26-f884-11ec-aa0a-42010a83ae55,0cb88e48-03d3-11eb-9d67-42010a83ae35
      disallowLaunchByUser: false
      instructions: Test instructions here
      approvalInputs: |
        str_req_def:
          type: string
          default: def1
          required: true
        bool_req_def:
          type: boolean
          default: true
          required: true
        num_req_def:
          type: number
          default: 9999
          required: true
        ch_opt_def:
          type: choice
          options:
            - op1
            - op2
          default: op2
        str_opt_no_def:
          type: string
    timeout-minutes: 4320
    delegates: cloudbees-io/manual-approval/.cloudbees/testing/custom-job.yml@CBP-8570_Bad_request
  print_outputs:
    steps:
      - name: Print manual approval output params
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "Output from custom job:"
          echo "comments: ${{ needs.ask-now.outputs.comments }}"
          echo "approvalInputValues: ${{ needs.ask-now.outputs.approvalInputValues }}"
    needs: ask-now
